config BR2_PACKAGE_KM_FRITZCAPI
	bool "km_fritzcapi"
	depends on BR2_i386
	help
	  Provides common IA-32 driver sources for the AVM FRITZ!ISDN
	  adapters.

comment "km_fritzcapi requires a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
