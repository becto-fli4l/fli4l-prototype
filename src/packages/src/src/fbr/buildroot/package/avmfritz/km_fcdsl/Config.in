config BR2_PACKAGE_KM_FCDSL
	bool "km_fcdsl"
	depends on BR2_i386
	help
	  Provides common IA-32 driver sources for the AVM FRITZ!DSL
	  adapters.

comment "km_fcdsl requires a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
