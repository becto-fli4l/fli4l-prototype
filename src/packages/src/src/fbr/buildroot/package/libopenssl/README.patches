openssl.mk.diff (kristov)
* Builds OpenSSL for the host, needed e.g. by host-m2crypto.

9001-no-build-env.patch (kristov)
* Prevents the build date/time and the full path to the compiler used from
  being compiled in.
