rsync.mk.diff (kristov)
* Explicitly declares rsync to be dependent on libiconv, as otherwise rsync
  may or may not depend on libiconv each time the FBR is rebuilt.
* Fixes NOBODY_USER to nobody and NOBODY_GROUP to nogroup, as these
  identifier are used on the fli4l router.
