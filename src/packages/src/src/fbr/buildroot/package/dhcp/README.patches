dhcp.mk.diff (kristov)
* Properly configures the paths to the DHCP(v6) lease and PID files.
* Adds --disable-failover, --disable-execute, and --disable-tracing to
  configure call for smaller binaries.
* Adds a dependency to dhcp-bind.
