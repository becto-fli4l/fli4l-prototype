9001-no-gperf.patch (kristov)
* Disables the use of gperf as it makes the binary larger. Additionally,
  the resulting binary may differ from build host to build host, depending
  on the existence of gperf.
