lprng-make.patch (Gentoo)
* Prevents make from segfaulting.

lprng-shell.patch (kristov)
* Force SHELL to /bin/bash instead of /bin/sh as otherwise the build process
  aborts when using some shell other than bash (e.g. dash).
