bind.mk.diff (kristov)
* Disables the use of the GSSAPI library.
* Disables the use of OpenSSL's GOST algorithm.
* Removes RPATH from binaries built.
* Prevents the build time from being compiled in.

9001-bind-no-build-env.patch (kristov)
* Prevents information about the build environment from being compiled in.
