0001-parallel-make.patch (kristov)
* Fixes a missing dependency that manifests itself when building in parallel.

0002-configure-libsdl.patch (kristov)
* Prevents SDL's compile flags from being used if use of SDL is disallowed.

0003-configure-libusb.patch (kristov)
* Fixes a cross-compiling issue related to the usage of libusb.
