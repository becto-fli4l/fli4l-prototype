wireless_tools.mk.diff (kristov)
* If libiw.so is wanted (the default), iwmulticall linked statically to libiw
  is not built as it misses necessary tools as iwevent. Instead, separate
  tools are built and linked dynamically to libiw.so.

wireless_tools-29-makefile.patch (kristov)
* Passes LDFLAGS to CC when linking.
