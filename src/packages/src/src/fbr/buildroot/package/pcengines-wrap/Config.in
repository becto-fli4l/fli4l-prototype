config BR2_PACKAGE_PCENGINES_WRAP
	bool "pcengines-wrap"
	depends on BR2_LINUX_KERNEL
	depends on BR2_i386
	help
	  drivers for PC-Engines WRAP

comment "pcengines_wrap requires a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
