wget.mk.diff (kristov)
* Explicitly declares wget to be dependent on libiconv, as otherwise wget may
  or may not depend on libiconv each time the FBR is rebuilt.

wget-no-build-env.patch (kristov)
* Prevents the full path to the compiler used from being compiled in.
