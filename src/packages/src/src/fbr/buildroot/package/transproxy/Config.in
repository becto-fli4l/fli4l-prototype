config BR2_PACKAGE_TRANSPROXY
	bool "transproxy"
	help
	  transproxy is used in conjunction with the Linux transparent proxy
	  feature (ipfwadm, ipchains, iptables), to transparently proxy HTTP
	  requests.
