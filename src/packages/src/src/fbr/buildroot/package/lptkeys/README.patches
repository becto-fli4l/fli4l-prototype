lptkeys-c-string.patch (kristov)
* Fixes unlimited string literals.

lptkeys-makefile.patch (kristov)
* Passes LDFLAGS to CC when linking.

lptkeys-parport.patch (kristov)
* Changes asm/io.h to sys/io.h (for inb and outb).
* Allows to use any parport device given its index (specifying physical port
  address is not needed/allowed anymore).
