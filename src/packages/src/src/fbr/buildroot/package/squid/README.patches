squid.mk.diff (kristov)
* Prevents 'rpath' from containing a reference to the build directory.
* Enables IDENT lookups.

9001-dlsearch-path.patch (kristov)
* Prevents the host's library search path to be compiled into the squid
  binary.
