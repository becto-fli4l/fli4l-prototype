libiconv.mk.diff (kristov)
* Adds --with-sysroot option to configure call. This prevents libtool from
  linking 'iconv' to the build host's libraries.
* Prevents 'rpath' from containing a reference to the build directory.
