# $Id: hwmon-soekris-net4801.inc 51847 2018-03-06 14:55:07Z kristov $
# Do not edit! This file is automaticly generated by hwmon-inc.xsl

hwmon_soekris_net4801_temp ()
{
    var1=`sensors -u pc87366-isa-6620 | grep temp3_input | cut -d : -f 2`
    var1=`echo ${var1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`

    show_tab_header ${_HWSUPP_RT_TEMP} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_TEMP}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU}</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}

hwmon_soekris_net4801_voltage ()
{
    var1=`sensors -u pc87366-isa-6620 | grep in1_input | cut -d : -f 2`
    var1=`echo ${var1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var2=`sensors -u pc87366-isa-6620 | grep in2_input | cut -d : -f 2`
    var2=`echo ${var2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var3=`sensors -u pc87366-isa-6620 | grep in3_input | cut -d : -f 2`
    var3=`echo ${var3}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var4=`sensors -u pc87366-isa-6620 | grep in4_input | cut -d : -f 2`
    var4=`echo ${var4}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var5=`sensors -u pc87366-isa-6620 | grep in5_input | cut -d : -f 2`
    var5=`echo ${var5}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var6=`sensors -u pc87366-isa-6620 | grep in7_input | cut -d : -f 2`
    var6=`echo ${var6}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var7=`sensors -u pc87366-isa-6620 | grep in8_input | cut -d : -f 2`
    var7=`echo ${var7}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var8=`sensors -u pc87366-isa-6620 | grep in9_input | cut -d : -f 2`
    var8=`echo ${var8}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var9=`sensors -u pc87366-isa-6620 | grep in10_input | cut -d : -f 2`
    var9=`echo ${var9}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`

    show_tab_header ${_HWSUPP_RT_VOLTAGE} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_VOLTAGE}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VCORE}</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VCC}</td>"
    echo "<td>${var2}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VPWR}</td>"
    echo "<td>${var3}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V12}</td>"
    echo "<td>${var4}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V12M}</td>"
    echo "<td>${var5}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>Vsb</td>"
    echo "<td>${var6}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>Vdd</td>"
    echo "<td>${var7}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VBAT}</td>"
    echo "<td>${var8}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>AVdd</td>"
    echo "<td>${var9}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}

hwmon_soekris_net4801 ()
{
    hwmon_soekris_net4801_temp
    hwmon_soekris_net4801_voltage
}

# _oOo_
