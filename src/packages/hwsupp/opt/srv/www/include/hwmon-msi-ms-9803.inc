# $Id: hwmon-msi-ms-9803.inc 51847 2018-03-06 14:55:07Z kristov $
# Do not edit! This file is automaticly generated by hwmon-inc.xsl

hwmon_msi_ms_9803_frequency ()
{
    var1=`cat /sys/devices/system/cpu/cpu0/cpufreq/cpuinfo_cur_freq`
  # var1=`cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq`
    var1=$((var1 / 1000))
    var2=`cat /sys/devices/system/cpu/cpu1/cpufreq/cpuinfo_cur_freq`
  # var2=`cat /sys/devices/system/cpu/cpu1/cpufreq/scaling_cur_freq`
    var2=$((var2 / 1000))

    show_tab_header ${_HWSUPP_RT_FREQUENCY} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_FREQUENCY}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU} 0</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU} 1</td>"
    echo "<td>${var2}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}


hwmon_msi_ms_9803_coretemp ()
{
    var_coretemp2=`sensors -u coretemp-isa-0000 | grep temp2_input | cut -d : -f 2`
    var_coretemp2=`echo ${var_coretemp2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`
    var_coretemp3=`sensors -u coretemp-isa-0000 | grep temp3_input | cut -d : -f 2`
    var_coretemp3=`echo ${var_coretemp3}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`
    show_tab_header Core-${_HWSUPP_TEMP} no
    echo "<table class='normtable'>"

    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>$Core-${_HWSUPP_RL_TEMP}</th>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU} 0</td>"
    echo "<td>${var_coretemp2}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU} 1</td>"
    echo "<td>${var_coretemp3}</td>"
    echo "</tr>"

    echo "</table>"
    show_tab_footer
}


hwmon_msi_ms_9803_w83627dhg_temp ()
{
    var_temp2=`sensors -u w83627dhg-isa-0a10 | grep temp2_input | cut -d : -f 2`
    var_temp2=`echo ${var_temp2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`
    var_temp3=`sensors -u w83627dhg-isa-0a10 | grep temp3_input | cut -d : -f 2`
    var_temp3=`echo ${var_temp3}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`
    var_temp1=`sensors -u w83627dhg-isa-0a10 | grep temp1_input | cut -d : -f 2`
    var_temp1=`echo ${var_temp1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`

    show_tab_header MB-${_HWSUPP_TEMP} no
    echo "<table class='normtable'>"

    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_TEMP}</th>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU} 0</td>"
    echo "<td>${var_temp2}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU} 1</td>"
    echo "<td>${var_temp3}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_SYS}</td>"
    echo "<td>${var_temp1}</td>"
    echo "</tr>"

    echo "</table>"
    show_tab_footer
}

hwmon_msi_ms_9803_w83627dhg_fan ()
{
    var_fan2=`sensors -u w83627dhg-isa-0a10 | grep fan2_input | cut -d : -f 2`
    var_fan2=`echo ${var_fan2}| sed 's/[^0-9]*\([0-9]*\).*/\1/'`
    var_fan1=`sensors -u w83627dhg-isa-0a10 | grep fan1_input | cut -d : -f 2`
    var_fan1=`echo ${var_fan1}| sed 's/[^0-9]*\([0-9]*\).*/\1/'`

    show_tab_header MB-${_HWSUPP_FANSPEED} no
    echo "<table class='normtable'>"

    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_FANSPEED}</th>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU}</td>"
    echo "<td>${var_fan2}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_MB}</td>"
    echo "<td>${var_fan1}</td>"
    echo "</tr>"

    echo "</table>"
    show_tab_footer
}

hwmon_msi_ms_9803_w83627dhg_voltage ()
{
#   var_in0=`sensors -u w83627dhg-isa-0a10 | grep in0_input | cut -d : -f 2`
#   var_in0=`echo ${var_in0}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in1=`sensors -u w83627dhg-isa-0a10 | grep in1_input | cut -d : -f 2`
    var_in1=`echo ${var_in1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in2=`sensors -u w83627dhg-isa-0a10 | grep in2_input | cut -d : -f 2`
    var_in2=`echo ${var_in2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in3=`sensors -u w83627dhg-isa-0a10 | grep in3_input | cut -d : -f 2`
    var_in3=`echo ${var_in3}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in4=`sensors -u w83627dhg-isa-0a10 | grep in4_input | cut -d : -f 2`
    var_in4=`echo ${var_in4}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in5=`sensors -u w83627dhg-isa-0a10 | grep in5_input | cut -d : -f 2`
    var_in5=`echo ${var_in5}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in6=`sensors -u w83627dhg-isa-0a10 | grep in6_input | cut -d : -f 2`
    var_in6=`echo ${var_in6}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in7=`sensors -u w83627dhg-isa-0a10 | grep in7_input | cut -d : -f 2`
    var_in7=`echo ${var_in7}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var_in8=`sensors -u w83627dhg-isa-0a10 | grep in8_input | cut -d : -f 2`
    var_in8=`echo ${var_in8}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`

    show_tab_header MB-${_HWSUPP_VOLTAGE} no
    echo "<table class='normtable'>"

    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_VOLTAGE}</th>"
    echo "</tr>"
    
#   echo "<tr align='center'>"
#   echo "<td>in0 ????</td>"
#   echo "<td>${var_in0}</td>"
#   echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VCORE}</td>"
    echo "<td>${var_in1}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>AVCC ${_HWSUPP_RC_V3_3}</td>"
    echo "<td>${var_in2}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>3VCC ${_HWSUPP_RC_V3_3}</td>"
    echo "<td>${var_in3}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V12}</td>"
    echo "<td>${var_in4}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V5}</td>"
    echo "<td>${var_in5}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>VCC RAM</td>"
    echo "<td>${var_in6}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V3SB}</td>"
    echo "<td>${var_in7}</td>"
    echo "</tr>"

    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VBAT}</td>"
    echo "<td>${var_in8}</td>"
    echo "</tr>"

    echo "</table>"

    show_tab_footer
}

hwmon_msi_ms_9803 ()
{
    hwmon_msi_ms_9803_frequency
    hwmon_msi_ms_9803_coretemp
    hwmon_msi_ms_9803_w83627dhg_temp
    hwmon_msi_ms_9803_w83627dhg_fan
    hwmon_msi_ms_9803_w83627dhg_voltage
}

# _oOo_
