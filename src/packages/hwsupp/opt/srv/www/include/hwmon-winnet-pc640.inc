# $Id: hwmon-winnet-pc640.inc 51847 2018-03-06 14:55:07Z kristov $
# Do not edit! This file is automaticly generated by hwmon-inc.xsl

hwmon_winnet_pc640_temp ()
{
    var1=`sensors -u w83697hf-isa-0290 | grep temp2_input | cut -d : -f 2`
    var1=`echo ${var1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`
    var2=`sensors -u w83697hf-isa-0290 | grep temp1_input | cut -d : -f 2`
    var2=`echo ${var2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`

    show_tab_header ${_HWSUPP_RT_TEMP} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_TEMP}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CPU}</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_MB}</td>"
    echo "<td>${var2}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}

hwmon_winnet_pc640_voltage ()
{
    var1=`sensors -u w83697hf-isa-0290 | grep in0_input | cut -d : -f 2`
    var1=`echo ${var1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var2=`sensors -u w83697hf-isa-0290 | grep in2_input | cut -d : -f 2`
    var2=`echo ${var2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var3=`sensors -u w83697hf-isa-0290 | grep in3_input | cut -d : -f 2`
    var3=`echo ${var3}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var4=`sensors -u w83697hf-isa-0290 | grep in4_input | cut -d : -f 2`
    var4=`echo ${var4}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var5=`sensors -u w83697hf-isa-0290 | grep in7_input | cut -d : -f 2`
    var5=`echo ${var5}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`

    show_tab_header ${_HWSUPP_RT_VOLTAGE} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_VOLTAGE}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VCORE}</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V3_3}</td>"
    echo "<td>${var2}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V5}</td>"
    echo "<td>${var3}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V12}</td>"
    echo "<td>${var4}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_V5SB}</td>"
    echo "<td>${var5}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}

hwmon_winnet_pc640 ()
{
    hwmon_winnet_pc640_temp
    hwmon_winnet_pc640_voltage
}

# _oOo_
