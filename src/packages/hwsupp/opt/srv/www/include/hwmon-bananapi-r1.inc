# $Id: hwmon-bananapi-r1.inc 51847 2018-03-06 14:55:07Z kristov $
# Do not edit! This file is automaticly generated by hwmon-inc.xsl

hwmon_bananapi_r1_temp ()
{
    var1=`sensors -u sun4i_ts-isa-0000 | grep temp1_input | cut -d : -f 2`
    var1=`echo ${var1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`
    var2=`sensors -u axp209-isa-0000 | grep temp1_input | cut -d : -f 2`
    var2=`echo ${var2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{1\}\).*/\1.\2/'`

    show_tab_header ${_HWSUPP_RT_TEMP} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_TEMP}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_SOC}</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_CHIP}</td>"
    echo "<td>${var2}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}

hwmon_bananapi_r1_voltage ()
{
    var1=`sensors -u axp209-isa-0000 | grep in0_input | cut -d : -f 2`
    var1=`echo ${var1}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var2=`sensors -u axp209-isa-0000 | grep in1_input | cut -d : -f 2`
    var2=`echo ${var2}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var3=`sensors -u axp209-isa-0000 | grep in2_input | cut -d : -f 2`
    var3=`echo ${var3}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`
    var4=`sensors -u axp209-isa-0000 | grep in3_input | cut -d : -f 2`
    var4=`echo ${var4}| sed 's/[^0-9]*\([0-9]*\)\.\([0-9]\{3\}\).*/\1.\2/'`

    show_tab_header ${_HWSUPP_RT_VOLTAGE} no

    echo "<table class='normtable'>"
    echo "<tr>"
    echo "<th>${_HWSUPP_HWMON_SENSOR}</th>"
    echo "<th>${_HWSUPP_RL_VOLTAGE}</th>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_ACIN}</td>"
    echo "<td>${var1}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VBUS}</td>"
    echo "<td>${var2}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_VBAT}</td>"
    echo "<td>${var3}</td>"
    echo "</tr>"
    echo "<tr align='center'>"
    echo "<td>${_HWSUPP_RC_APS}</td>"
    echo "<td>${var4}</td>"
    echo "</tr>"
    echo "</table>"

    show_tab_footer
}

hwmon_bananapi_r1 ()
{
    hwmon_bananapi_r1_temp
    hwmon_bananapi_r1_voltage
}

# _oOo_
