Packaging:

    mktarball
    mkdoc-links.sh 
    mkpackages.sh
    mktarball.sh
    mkclean.sh

Consistency checks:

    check-files.pl
    check_svn_prop.sh
    resolve-libs.sh (used by check-files.pl)

Consistency enforcement tools:

    correct-executable-flag.sh
    sort-files_txt.sh

Test tools:

    _mkfli4lsvn.sh - Creates a fli4l build directory and populates it
                     with copies of files in the repository while performing
                     the same conversion as the packaging process
